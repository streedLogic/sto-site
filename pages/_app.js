import "../styles/globals.css";
import "../styles/fonts.scss";
import { ServerStyleSheet } from "styled-components";

function StoSite({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default StoSite;
